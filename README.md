# GraphQL Template

Quick start for React/GraphQL projects.

to test locally:

```
cd sub/data
sls offline start
```

then, in another terminal window:

```
cd sub/app
yarn start
```

(not working yet)

to be resolved:
- ApolloClient usage